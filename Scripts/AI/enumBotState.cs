public enum enumBotState {
    
    //Mudar o (int)valor tem efeito (animatorController) que terá que ser ajustado manualmente
    Aim = 0, Covering, Flank, Idle, Melee,
    Reposition = 5, ShortestSight, StraightPath, Stagger

}